import { GooglePlacesKey } from "./apiKey";

const rootURLPlaces = `https://maps.googleapis.com/maps/api/place/${GooglePlacesKey}`;
const rootURLGeoCodes = `http://maps.googleapis.com/maps/api/geocode/json?latlng=`;

export {rootURLPlaces, rootURLGeoCodes}

